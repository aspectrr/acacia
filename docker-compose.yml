services:
  acacia:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - acacia-db
    environment:
      - DATABASE_URL=postgresql://acacia:acacia_password@acacia-db:5432/acacia
      - NODE_ENV=production
    volumes:
      - ./:/app
      - /app/node_modules
    restart: unless-stopped

  acacia-db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=acacia
      - POSTGRES_PASSWORD=acacia_password
      - POSTGRES_DB=acacia
    volumes:
      - acacia_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U acacia"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  acacia_data:
